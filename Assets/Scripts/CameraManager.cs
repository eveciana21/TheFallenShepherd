using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class CameraManager : MonoBehaviour
{
    /*  [SerializeField] private GameObject[] _camArray;

    public void ResetCams()
     {
         foreach (var cam in _camArray)
         {
             cam.GetComponent<CinemachineVirtualCamera>().Priority = 10;
         }
     }

    public void SelectMasterCam(int cam)
    {
        _camArray[cam].GetComponent<CinemachineVirtualCamera>().Priority = 15;
    }

    */
}
